PORT=5000
JWT_SECRET=ganesh_chaturthi_2026_super_secret_key_change_this
JWT_EXPIRES_IN=12h

# Default admin account created automatically the first time the server runs
# (only if no users exist yet). Change the password immediately after first login.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=ChangeMe@123
DEFAULT_ADMIN_NAME=Committee Admin
